local icons = require("icons")
local colors = require("colors")
local settings = require("settings")

sbar.add("alias", "Kontrollzentrum,FocusModes", {
    position = "right",
})

sbar.add("alias", "Kontrollzentrum,Bluetoot", {
    position = "right",
})

sbar.add("alias", "Docker Desktop,Item-0", {
    position = "right",
})